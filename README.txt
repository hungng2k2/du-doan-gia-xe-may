Hướng dẫn trình tự chạy chương trình:
B1: Để crawl data xe máy cũ chúng ta vào file crawl.ipynb để chạy toàn bộ file jupiter notebook 
(Tham số number_size để thiết lập số lượng mẫu cần crawl, page để thiết lập trang đầu tiên bắt đầu crawl)

B2: Làm sạch dữ liệu crawl => Vào file clean_data để chạy toàn bộ file jupiter notebook

B3: Trực quan hóa dữ liệu => Vào file eda để chạy toàn bộ file jupiter notebook

B3: Để tìm các feature engineering cho đề tài và bộ siêu tham số tối ưu của mô hình
     => Vào file find_best_feature_engineering để chạy toàn bộ file jupiter notebook
    
B3: Huấn luyện và đánh giá mô hình
     => Vào file model_building để chạy toàn bộ file jupiter notebook